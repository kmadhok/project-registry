.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.DS_Store
*.egg-info/
/build/
dist/

# Observed GitHub state is a refreshable cache, not a source of truth.
# Proposals and the audit log stay tracked so change history is durable.
data/github/
data/portfolio.json

# Ephemeral autonomous-builder coordination files. Journal, state, and
# digests remain tracked.
data/build/lease.json
data/build/STOP

# Codex marathon runner state (docs/codex-marathon/ is tracked; logs are not)
.codex-marathon/
.marathon-stop
