{
  "mcpServers": {
    "project-registry": {
      "command": ".venv/bin/python",
      "args": ["-m", "project_registry.cli", "--root", ".", "mcp"],
      "env": { "PYTHONPATH": "src" }
    }
  }
}
